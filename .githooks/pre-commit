#!/usr/bin/env bash
set -euo pipefail
if ! command -v gitleaks >/dev/null 2>&1; then
  echo "ERROR: gitleaks not installed. Refusing to commit." >&2
  exit 1
fi
gitleaks protect --staged --redact -v --config "$(git rev-parse --show-toplevel)/.gitleaks.toml"
