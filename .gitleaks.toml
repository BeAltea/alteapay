# gitleaks config — default rules plus allowlist for documented placeholders.
[extend]
useDefault = true

[allowlist]
description = "Documented placeholders and local-only example values"
regexes = [
  '''PLACEHOLDER[A-Z0-9_]*''',
  '''PLACEHOLDER_LOCAL_ONLY''',
]
paths = ['''\.env\.example$''']
