# Contrato T4 — Canal e-mail (convite para `email_only`)

**Dev dono:** `dev-T4` · **Fase C** · **Valida:** `vt-security` (supressão/PII), `vt-correctness`, `vn-comunicacao`.

## Arquivos exclusivos
- `lib/journey/email-dispatch.ts`
- `lib/notifications/templates/journey-invite.ts` (+ `.html`/render)
- testes `tests/journey/email-dispatch.test.ts`

**Exporta para a T2:** `dispatchEmailInvite({companyId, customerId, token, mode})`. T2 chama; T4 não edita `campaigns.ts`. Reusa o cliente SendGrid existente (`lib/notifications/sendgrid.ts`) e a fila `alteapay-email` — **não recria**.

## Escopo (§6/T4 + E2 + §4)
1. Convite por e-mail para `contact_profile='email_only'`: assunto e corpo **sem valor e sem detalhe da dívida** (E8/E9 estendido ao e-mail); marca **AlteaPay como parceira do cedente** (o nome do credor pode aparecer no e-mail? → **NÃO** nesta onda, para manter "credor só após login"; usar "seu credor"/"parceira oficial de cobrança"). Link `/c/{token}` + opt-out (link já especificado).
2. `EMAIL_SEND_MODE=inline|queue` (default `queue`). `inline` = SendGrid direto com rate-limit + teto por request (piloto sem worker/Upstash). `queue` = `alteapay-email`.
3. **Supressão dupla antes de enviar:** `contact_suppressions` próprio **E** listas do SendGrid (bounces/blocks/invalid/spam/unsubscribes). Suprimido → `suppressed` com motivo, **não envia**.
4. Registrar `message.sent`/`message.failed` com `channel='email'`. Se o webhook de eventos SendGrid existir, alimentar `delivered`/`opened` com `provider_status_source='sendgrid'`; senão deixar preparado e **documentar**.

## Critérios de pronto
- Nenhum e-mail para suprimido (em qualquer caminho); `vt-security` confirma.
- Sem valor/dívida no conteúdo; opt-out funcional; tom respeitoso (`vn-comunicacao`).
- `inline` respeita teto + rate-limit; testes verdes.
