# Contrato T1 — Dados, perfil de contato e projeção de status

**Dev dono:** `dev-T1` · **Bloqueante:** SIM (Fase B; T2–T6 dependem dos nomes/formato) · **Valida:** `vt-data` + `vt-correctness` (nunca o próprio dev).

## Arquivos que ESTE dev pode escrever (exclusivo)
- `supabase/migrations/2026092X_campaign_status_contact_profile.sql`
- `supabase/migrations/2026092X_campaign_status_negotiation_state.sql`
- `lib/journey/contact-profile.ts`
- `lib/journey/negotiation-state.ts`
- `ops/chatbot-journey-2026-09/reports/T1_dados.md`
- testes em `tests/journey/contact-profile.test.ts`, `tests/journey/negotiation-state.test.ts`

Precisou tocar `lib/journey/campaigns.ts` (ex.: compartilhar `toE164Mobile`) → **abrir request**, não editar.

## Escopo (§6/T1 do prompt)
1. **Perfil de contato materializado** em `customers`: `mobile_e164`, `email_valid`, `contact_profile` (`mobile|email_only|both|none`), índices `(company_id, contact_profile)` e `(company_id, mobile_e164)`.
   - `mobile_e164`: **reusar a regra de `toE164Mobile`** de `campaigns.ts` (não duplicar). Fonte `customers.phone`; fallback `VMAX."Telefone 1"/"Telefone 2"` (isolamento por `id_company`).
   - `email_valid`: regex conservador + rejeitar placeholder (`naotem*`), domínio interno do cedente e supressões conhecidas.
   - Preferir **generated column STORED** se a expressão for imutável; senão coluna comum + `recompute_contact_profile(company_id)` idempotente + trigger. **Documentar o caminho escolhido e por quê.**
2. **Projeção `negotiation_state`** `(company_id, customer_id)`: `stage`, `stage_rank`, `stage_at`, `marks jsonb`, `channel`, `campaign_id`, `session_id`, `agreement_id`, `has_live_charge`, `provider_status_source`, `updated_at`. Índices: `(company_id, stage)`, `(company_id, stage_rank desc)`, `(company_id, updated_at desc)`, `(campaign_id)`.
   - `applyJourneyEventToState(event)` chamado sempre que grava `journey_events` (mesma transação quando possível) + `rebuildNegotiationState(companyId)` idempotente. Mapa evento→estágio conforme **Apêndice A** e a lista real de tipos no diagnóstico §1.
   - **Estágio nunca regride** por evento de canal; só por evento de domínio (cancelamento/desfazimento). `stage_rank` monotônico.
   - `has_live_charge`: reusar a leitura do guard de idempotência (`lib/asaas-idempotency.ts`) — não duplicar.
3. **Backfill** idempotente dos ~977 devedores VMAX (+ demais), com contagem antes/depois.

## Critérios de pronto
- Migrations **aditivas**, aplicáveis do zero e sobre dump de prod; RLS por `company_id` nas tabelas novas.
- `rebuildNegotiationState` == projeção incremental (diferença ZERO — `vt-data` exige).
- `reports/T1_dados.md`: nomes reais de colunas usadas, distribuição por `contact_profile` e por `stage` por tenant, EXPLAIN das 2 consultas principais da página (para T5).
- Testes verdes; sem erro TS novo nos arquivos da trilha.

## Dependências / entrega para as outras
Entrega os **nomes e o formato** que T2 (filtros), T5 (colunas/filtros da página) e T6 (estágio no contexto n8n) vão consumir. Nenhuma trilha da Fase C começa antes do GATE B.
