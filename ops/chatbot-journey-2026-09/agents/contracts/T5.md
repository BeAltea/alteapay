# Contrato T5 — Página de negociações (status por devedor)

**Dev dono:** `dev-T5` · **Fase C** · **Valida:** `vt-correctness`, `vt-data` (plano de query), `vn-operacao`.

## Arquivos exclusivos
- `app/super-admin/negociacoes/*` (NOVA rota — não colidir com `negociacao-ia`)
- `app/api/super-admin/negociacoes/route.ts` (lista paginada server-side)
- `components/super-admin/negotiations/*`
- testes `tests/super-admin/negociacoes.test.ts`

**Consome (não escreve):** `negotiation_state` + `contact_profile` de **T1** (via leitura). Precisou de coluna nova na projeção → **request para T1**.

## Escopo (§6/T5 + E4/E5 + Apêndice A)
1. **Lista:** devedor (nome + **doc mascarado**), cedente, `contact_profile` (ícone), canal usado, **estágio** (badge + data), última atividade, valor em aberto, cobrança viva (sim/não), ações (detalhe, reenviar, copiar link, gerar planilha da seleção).
2. **Filtros combináveis (na URL):** cedente, estágio (multi), `contact_profile` (`mobile|email_only|both|none`), canal, campanha, faixa aging, faixa valor, cobrança viva, supressão, período de última atividade, busca.
3. **Contadores no topo** por estágio; total = soma (assert visível em teste).
4. **Detalhe do devedor:** timeline (`journey_events` com data + **procedência**), transcrição, reconhecimento, ofertas, acordo, pagamentos, casos, mensagens por canal.
5. **Honestidade de dados (E4):** entrega/leitura mostram **"não informado pelo provedor"** quando `provider_status_source='none'` (tooltip). Nunca inventar entrega.
6. **`in_chat` vs `replied_whatsapp` (E5)** distinguidos; padrão exibido = `in_chat` (janela `CHAT_ACTIVE_WINDOW_MIN`).
7. Paginação + ordenação (estágio / última atividade) server-side; export CSV da visão filtrada (mínimo necessário, registrado).

## Critérios de pronto
- `vt-data`: as 2 consultas principais usam índices de T1 (sem N+1, volume 3196).
- Contadores fecham com a lista; `super_admin` obrigatório; doc sempre mascarado.
- `vn-operacao`: operador seleciona 10 com celular + sem cobrança viva e gera planilha em ≤3 cliques; estágio compreensível sem treino.
