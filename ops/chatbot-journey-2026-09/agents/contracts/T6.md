# Contrato T6 — Contratos n8n e pagamento

**Dev dono:** `dev-T6` · **Fase C** · **Valida:** `vt-correctness`, `vn-cobranca`, `vt-security`.

## Arquivos exclusivos
- `lib/journey/payment-actions.ts` (ESTENDER)
- `app/api/webhooks/n8n/route.ts` (ESTENDER — já tem `payment.create`/`payment.status`/`chat.send`/`prompt.*`)
- `docs/N8N_TEAM_INTEGRATION_GUIDE.md`, `docs/N8N_FLOW_REQUIREMENTS.md` (atualizar)
- testes `tests/journey/payment-actions.*.test.ts`, `tests/webhooks/n8n.*.test.ts`

**Fronteira:** NÃO mexer em `engine.ts` (contexto ao n8n) além do necessário; se precisar do estágio no contexto e ele estiver em `context.ts`/`engine.ts`, **abrir request**. `has_live_charge`/estágio vêm de T1.

## Escopo (§6/T6 + §4)
1. `payment.create` **idempotente por `(session_id, offer_id)`** (já existe) + `already_charged` (409) — **confirmar** com testes de dupla-chamada.
2. **Reenvio do link existente (§4):** `payment.status` deve **devolver as URLs do acordo vivo** (PIX/boleto/invoice) para o caso `already_charged` — o front/n8n reenvia em vez de gerar nova cobrança. Garantir que `fetchPaymentDetails` cobre isso.
3. `chat.send`, `prompt.ask`, `prompt.close` (onda R) — manter/robustecer.
4. **Contexto ao n8n** com `debt_acknowledgement`, `active_prompt`, `debt.ids[]` e o **estágio atual** (`negotiation_state.stage` de T1).
5. **Doc do time n8n:** adicionar o novo campo `stage` no contexto, o caso `already_charged` → recomendação de usar `payment.status`, e o modo `processing` (sem worker → `{status:'processing', poll_after_ms}`).

## Critérios de pronto
- `vn-cobranca`: nunca 2 cobranças; ASAAS é a fonte da verdade; `already_charged` → reenvio, não nova cobrança.
- Idempotência provada (rodar 2x, payload idêntico, zero cobrança nova).
- Guard duplo (`lib/asaas-idempotency.ts`) intacto em todo caminho; testes verdes.
- Documento do time n8n atualizado e consistente com o runtime.
